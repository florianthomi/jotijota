{% extends 'layout.html.twig' %}

{% block top_title %}{{ "title.edition_new"|trans }}{% endblock %}

{% block buttons %}
    {{ component('Link', {
        icon: {name: 'icon-back'},
        label: 'label.back'|trans,
        action: path('app_edition_index'),
        class: 'bg-primary text-white'
    }) }}
{% endblock %}

{% block content %}
<div class="block-container">
        {{ component('EditionForm', {
            form: form,
            edition: edition,

        }) }}
</div>
{% endblock %}
