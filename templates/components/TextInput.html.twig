<div{{ attributes.defaults({ class: 'mb-4'}) }}>
    {{ form_label(form, null, {'label_attr': {'class': 'form-labe'}}) }}
    <div class="relative">
        {{ form_widget(form, {'attr': {'class': 'form-input'}}) }}
        <span class="absolute right-4 top-4">{% block icon %}{% endblock %}</span>
    </div>
</div>
