{% for type, messages in app.flashes %}
    {% for message in messages %}
        {{ component('Alert', {
            type: type,
            title: message
        }) }}
    {% endfor %}
{% endfor %}
